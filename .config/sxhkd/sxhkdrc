super + Return
	terminal

shift + super + Return
	terminal -d

control + Return
	terminal -e 'cd $(fzf-choose-favorite -s); clear'

super + r
	modal-run -matching normal

super + shift + r
	modal-run --terminal

shift + Delete
	kill-focus

super + control + s
	browser

super + control + w
	todoist

super + control + d
	upwork

super + b
	clipboard-sync-trim

super + F1
	screenshot

super + F2
	screenshot deepin

control + F10
	brightness toggle

super + 8
	volume down

super + 9
	volume up

alt + control + d
	notification-information-basic

super + control + e
	translator

super + control + t
	toggle translator-watcher

super + Tab
	quicknote

super + 5
	carcosa-ui

super + 6
	carcosa-new-password

super + 7
	carcosa-qr

control + Delete
	environment-setup

super + grave
	kuake-bottom

super + backslash
	kuake --focus --center --middle --height 90 --width 75

super + equal
	kuake --terminal --class top --top --middle --height 30 --width 50

control + F5
	upwork-switch

control + F6
	VOLUME=30000 headset

control + F7
	openvpn-toggle

control + F8
	brightness down

control + F9
	brightness up

control + F11
	jidle-toggle

control + F12
	SUSPEND_NOTIFY_TIMEOUT=60 upwork-suspend

control + Pause
	screen-lock -suspend

control + super + Print
	screen-lock -suspend

shift + F1
	chrome-netflix
