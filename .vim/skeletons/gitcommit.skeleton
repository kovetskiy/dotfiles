`!p
import subprocess
import vim

_line_number, _column_number = vim.current.window.cursor

_buffer = vim.current.buffer
_line   = _buffer[_line_number - 1]

if _line == "":
    _branch = subprocess.check_output([
        "git", "symbolic-ref", "HEAD"
    ]).strip()

    _matches = re.search('([a-zA-Z]+\-[0-9]+)', _branch)
    if _matches != None:
        _branch = _matches.group(1)
        snip.rv = _branch + ": "
`
