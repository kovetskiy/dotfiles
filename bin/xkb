#!/bin/bash

xkbcomp -w 0 -I$HOME/.xkb -R$HOME/.xkb $HOME/.xkbrc $DISPLAY

{ sleep 1 && xset r rate 110 150; } &
xset -b

sudo pkill shift-shift
sudo shift-shift -first CAPSLOCK -second RIGHTSHIFT -match razer &
sudo shift-shift -first CAPSLOCK -second RIGHTSHIFT -match keyboard &

disown
