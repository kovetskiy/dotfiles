#!/bin/bash
notify-send -t 1000 -- "`date`"
notify-send -t 1000 -- "`weather`" "`sensors \
        | tail -n +3 | head -n -1 | head -n 2 \
        | sed 's/       / /g' \
        | awk '{print $1" "$2}'`"
