#!/bin/bash

text=$(i3-input -F '# %s' | tail -n 3 | grep output | cut -d'=' -f2 | tr -s ' ' '+')
url="http://translate.yandex.net/api/v1/tr.json/translate?lang=en-ru&text=$text&srv=tr-text"
tr=$(curl -s "$url" | jq .text[0] | sed 's/"//g')
notify-send -t 1000 "$tr"
