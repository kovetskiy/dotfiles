#!/bin/bash

acpi_listen | while read -r line
do
    action=`echo $line | awk {'print \$1'}`

    case "$action" in 
        "button/volumeup")
            volume up
            ;;
        "button/volumedown")
            volume down
            ;;
    esac
done
