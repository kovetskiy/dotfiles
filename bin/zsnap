#!/bin/bash

which zfs >/dev/null
if [ $? -ne 0 ]; then
    exit 0
fi

set -e -u

pool="zroot"
name=`date | sed 's/  / /g' | sed 's/ /_/g' `
sudo zfs snapshot $pool@$name

echo "ZFS Snapshot [$pool@$name] created"
