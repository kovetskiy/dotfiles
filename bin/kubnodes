#!/bin/bash

context="${1}"
if [[ ! "$context" ]]; then
    echo "$0 <context>"
    exit 1
fi

kubectl --context "${context}" get nodes -o template --template='{{range.items}}{{.metadata.name}} {{range.status.addresses}}{{if eq .type "InternalIP"}}{{.address}}{{end}}{{end}}{{"\n"}}{{end}}'
