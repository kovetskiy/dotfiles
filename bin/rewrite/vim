#!/bin/bash

if [[ $# -eq 1 ]]; then
    if ! grep "^[\-+]" <<< "$1"; then
        if [[ ! -f "$1" ]]; then
            file=$(/bin/ls -t $1* | head -n1)
        fi
    fi
fi

exec /bin/vim --servername VIM "${file:-$@}"
