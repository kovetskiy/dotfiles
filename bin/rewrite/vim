#!/bin/bash

if [[ "$1" ]]; then
    if ! grep -q "^[-+]" <<< "$1"; then
        if [[ ! -f "$1" ]]; then
            file=$(/bin/ls -t $1* 2>/dev/null | head -n1)
        fi
    fi
fi

#if [[ -f /bin/nvim ]]; then
#    exec /bin/nvim ${file:-"$@"}
#fi

exec /bin/vim ${file:-"$@"}
