#!/bin/bash

acpi_listen | while read -r line
do
    action=`echo $line | awk {'print \$1'}`

    case "$action" in 
        "button/volumeup")
            ~/bin/volume up
            ;;
        "button/volumedown")
            ~/bin/volume down
            ;;
    esac
done
