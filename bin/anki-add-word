#!/bin/bash

word=$(i3-input -F '# %s' | tail -n 3 | grep output | cut -d'=' -f2)

if [[ "$word" == "" ]]; then
    exit 1
fi

tr=$(runki --dry <<< "$word")

notify-send "$tr" -t 2000

runki <<< "$word"
