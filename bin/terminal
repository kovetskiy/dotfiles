#!/bin/bash

PROFILE=$(cat ~/.config/profile)
if [[ "$PROFILE" == "laptop" ]]; then
    exec marvex -s --quiet -b urxvt --terminal '@path --title "@title" -e @command' -r 2 "${@}"
else
    urxvt || konsole
fi
