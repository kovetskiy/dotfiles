#!/bin/bash

PROFILE=$(cat ~/.config/profile)
if [[ "$PROFILE" == "laptop" ]]; then
    marvex --tmux-socket $TMUX_SOCKET -s --quiet -b urxvt --terminal '@path --title "@title" -e @command' -r 2
else
    konsole
fi
