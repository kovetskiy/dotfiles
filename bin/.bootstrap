#!/bin/bash

symlinks=($(cat ~/dotfiles/.symlinks))

:exec() {
    echo "$ $*"
    "$@"
}

for path in "${symlinks[@]}"; do
    :exec rm -rf ~/"$path"
    :exec ln -s ~/dotfiles/"$path" ~/"$path"
done
