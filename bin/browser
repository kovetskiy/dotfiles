#!/bin/bash

URL=$(sed -r 's#^(git+ssh|ssh|git)://(\w+@)?##' <<< "$1")
shift

if pgrep -f "firefox-beta" &>/dev/null; then
    UNIT="firefox-beta"
else
    UNIT="google-chrome-stable"
fi

if [[ "$UNIT" == "firefox" ]]; then
    exec firefox --noerrdialogs --disable-infobars "$URL" "$@"
else
    exec "$UNIT" "$URL" "$@"
fi
