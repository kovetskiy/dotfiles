#!/bin/bash

xwininfo=$(xwininfo ${1:+-id $1})
id=$(awk '/Window id/{print $4}' <<< "$xwininfo")
name=$(xdotool getwindowname $id)

xkill -id $id

if [[ $name == "marvex"* ]]; then
    tmux kill-window
fi
