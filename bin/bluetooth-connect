#!/usr/bin/expect -f

set prompt "#"

spawn sudo systemctl start bluetoothd.service
spawn sudo bluetoothctl -a
expect -re \$prompt
send "power on\r"
sleep 1
send "connect \t"
expect -re :
send "\r"
expect -re "Connection "
expect eof
