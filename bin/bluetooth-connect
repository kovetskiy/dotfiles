#!/usr/bin/expect -f

set prompt "#"

spawn sudo systemctl start bluetooth.service
spawn sudo bluetoothctl -a
send "power on\r"
sleep 1
send "connect \t"
expect -re :
send "\r"
expect -re "Connection "
expect eof
