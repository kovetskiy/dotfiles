fpath=($(xargs -n1 echo <<< "$fpath" | grep -iv /prompt))
fpath=(~/.zprezto/modules/prompt/functions/ $fpath)

zstyle ':prezto:*:*' color 'yes'

zstyle ':prezto:load' pmodule \
    'environment' \
    'terminal' \
    'editor' \
    'history' \
    'directory' \
    'completion' \
    'history-substring-search' \
    'git'

zstyle ':prezto:module:editor' key-bindings 'vi'

# fix me
source ~/.zprezto/modules/helper/init.zsh
source ~/.zprezto/modules/git/functions/git-info

source /usr/share/zsh/functions/Prompts/promptinit
autoload -Uz promptinit
promptinit
prompt lambda17

zstyle ':completion:*' rehash true
